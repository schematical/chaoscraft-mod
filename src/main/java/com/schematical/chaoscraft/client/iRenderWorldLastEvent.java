package com.schematical.chaoscraft.client;

import net.minecraftforge.client.event.RenderWorldLastEvent;

public interface iRenderWorldLastEvent {
    boolean onRenderWorldLastEvent(RenderWorldLastEvent event);
}
